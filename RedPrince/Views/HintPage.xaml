<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="RedPrince.Views.HintPage"
             Title="{Binding Title}">
    
    <VerticalStackLayout>
        
        <Grid Margin ="5, 20, 5, 5" Padding="5" RowSpacing="6" ColumnSpacing="12">

            <Grid.RowDefinitions>
                <RowDefinition Height = "Auto"/>
                <RowDefinition Height = "Auto"/>
                <RowDefinition Height = "Auto"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width = "*"/>
                <ColumnDefinition Width = "*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Row= "0"
				    Grid.Column= "0"
                    Grid.ColumnSpan="2"
				    Text ="{Binding Hint}"
				    Command="{Binding HintClickedCommand}" />

            <Button Grid.Row= "0"
				    Grid.Column= "1"
                    Grid.ColumnSpan="2"
				    Text ="{Binding Submit}"
				    Command="{Binding SubmitClickedCommand}" />

        </Grid>
        
    </VerticalStackLayout>
    
</ContentPage>